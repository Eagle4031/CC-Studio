# CLAUDE.md — CC-Studio

This file provides guidance for AI assistants working in this repository.

## Project Overview

CC-Studio is a new project. This repository is in its initial state and does not yet contain application code, build configuration, or dependencies.

## Repository Structure

```
CC-Studio/
├── CLAUDE.md          # AI assistant guidance (this file)
└── .git/              # Git repository
```

> **Note:** Update this section as the project grows with source directories, configuration files, and other artifacts.

## Development Setup

No build tools, package managers, or dependencies are configured yet. Update this section once the project scaffolding is in place with:

- Language/runtime version requirements
- Install commands (e.g., `npm install`, `pip install -r requirements.txt`)
- Environment variable setup
- Database or service dependencies

## Common Commands

<!-- Add commands as they become available, e.g.: -->
<!-- | Command | Description | -->
<!-- |---------|-------------| -->
<!-- | `npm run dev` | Start development server | -->
<!-- | `npm test` | Run test suite | -->
<!-- | `npm run build` | Production build | -->
<!-- | `npm run lint` | Lint and format check | -->

No commands configured yet.

## Code Conventions

Update this section once coding standards are established:

- Language and framework choices
- File and directory naming conventions
- Code style / linting rules
- Import ordering conventions
- Error handling patterns
- Testing conventions and coverage expectations

## Git Workflow

- Develop on feature branches prefixed with `claude/`
- Write clear, descriptive commit messages
- Keep commits focused on a single logical change

## Key Architecture Decisions

Document important architectural decisions here as they are made (e.g., state management approach, API design patterns, database choices).

## Things to Avoid

- Do not commit secrets, API keys, or credentials
- Do not add large binary files to the repository
- Do not make changes outside the scope of the current task
